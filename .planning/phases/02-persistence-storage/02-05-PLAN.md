---
phase: 02-persistence-storage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/passphrase.rs
autonomous: true
gap_closure: true
requirements: [STOR-01]

must_haves:
  truths:
    - "The transient rpassword plaintext buffer is zeroized on drop — the passphrase never persists in an un-zeroized String (honors the passphrase.rs module-doc invariant, D-01/D-04)."
    - "The new-store confirm-twice path still prompts twice, rejects a mismatch, and prints the unrecoverability warning; the unlock path still prompts once."
  artifacts:
    - src/store/passphrase.rs
  key_links:
    - "rpassword::prompt_password read → Zeroizing<String> → SecretString (no un-zeroized intermediate binding survives)"
---

<objective>
Close the single minor gap from 02-UAT.md (WR-01): the interactive production
passphrase reads in `src/store/passphrase.rs` land the plaintext in an
un-zeroized `String`, contradicting the module's own stated invariant
("Reads directly into a [`SecretString`] via `rpassword` so the passphrase is
never echoed and never lands in a plain `String`").

The maintainer decided FIX (not accept-and-document). Wrap each
`rpassword::prompt_password(...)` read in `Zeroizing<String>` so the transient
plaintext buffer is wiped on drop before it is handed to `SecretString`. This
one passphrase unlocks the identity key AND every share (D-02), so the memory
hygiene here is load-bearing.

Purpose: Make the code honor the invariant its own doc-comment promises.
Output: `src/store/passphrase.rs` with all three prompt reads zeroize-wrapped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-persistence-storage/02-UAT.md

# The file to fix. Note the established Zeroizing pattern already used
# throughout the store layer (identity.rs:37, crypto.rs:19, participant.rs:28,
# checkpoint.rs:53 all `use zeroize::Zeroizing;`). `zeroize = "1.9.0"` is
# already a direct dependency — no install task needed.
@src/store/passphrase.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zeroize the transient rpassword plaintext buffers</name>
  <files>src/store/passphrase.rs</files>
  <action>
Add `use zeroize::Zeroizing;` to the imports (mirror the pattern in
src/store/identity.rs, crypto.rs, participant.rs, checkpoint.rs — `zeroize`
1.9.0 is already a direct dependency, do not touch Cargo.toml).

In `impl PassphraseSource for InteractivePassphrase` (the `#[cfg(not(test))]`
block), wrap every `rpassword::prompt_password(...)` result in
`Zeroizing::new(...)` so the transient plaintext `String` is wiped on drop,
then hand an owned copy to `SecretString`:

- Unlock path (currently line ~83): bind the read as
  `Zeroizing::new(rpassword::prompt_password("Store passphrase: ")?)` and
  return `SecretString::from(<binding>.as_str().to_owned())`. The `.to_owned()`
  result is consumed immediately by `SecretString::from` (SecretString is
  zeroize-on-drop), so no un-zeroized binding survives.
- Confirm-twice path (currently lines ~92-97): wrap both reads
  (`first`, `second`) in `Zeroizing::new(...)`. Compare their dereferenced
  values (`*first != *second`) to keep the mismatch check working, since the
  comparison operates on the wrapped `String`s. On match, return
  `SecretString::from(first.as_str().to_owned())`.

Do NOT change the module doc-comment, the trait, `InCodePassphrase`, the
`confirm` flag semantics, the warning text, or the mismatch error
(`StoreError::Age("passphrases did not match".into())`). Do NOT introduce any
env/flag passphrase source (D-01/D-03). The fix is purely wrapping the three
transient reads.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && test "$(grep -c 'Zeroizing::new(rpassword::prompt_password' src/store/passphrase.rs)" = "3" && grep -q 'use zeroize::Zeroizing;' src/store/passphrase.rs && cargo test --lib store::passphrase 2>&1 | tail -5</automated>
  </verify>
  <done>
`cargo build` succeeds. All three `rpassword::prompt_password` reads in
`src/store/passphrase.rs` are wrapped in `Zeroizing::new(...)` (grep count == 3)
and `use zeroize::Zeroizing;` is imported. The existing in-code passphrase
round-trip test still passes. The unlock/confirm/mismatch/warning behavior is
byte-for-byte unchanged except that the transient plaintext buffer is now
wiped on drop.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal→process | The typed passphrase crosses from the TTY into process memory via `rpassword`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `InteractivePassphrase::passphrase` transient plaintext buffer | medium | mitigate | Wrap all three `rpassword::prompt_password` reads in `Zeroizing<String>` so the buffer is zeroized on drop; the owned copy is consumed directly by zeroize-on-drop `SecretString`. This is the WR-01 fix itself. |
| T-02-02 | Information Disclosure | passphrase acquisition surface | high | accept | No env/CLI-flag passphrase source ships (D-01/D-03); production path is interactive no-echo only. Unchanged by this fix — reaffirmed, not modified. |
</threat_model>

<verification>
- `cargo build` compiles cleanly.
- `grep -c 'Zeroizing::new(rpassword::prompt_password' src/store/passphrase.rs` == 3.
- `use zeroize::Zeroizing;` present in the file.
- `cargo test --lib store::passphrase` passes (in-code source round-trip still green).
- Module doc-comment invariant ("never lands in a plain `String`") is now truthful.
</verification>

<success_criteria>
The WR-01 gap is closed: the production interactive passphrase reads no longer
leave an un-zeroized plaintext `String` in memory, honoring the passphrase.rs
module-doc invariant, with the confirm-twice + mismatch + warning UX preserved.
</success_criteria>

<output>
Create `.planning/phases/02-persistence-storage/02-05-SUMMARY.md` when done.
</output>
