---
phase: 02-persistence-storage
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/store/mod.rs
  - src/store/manifest.rs
  - src/store/participant.rs
  - src/store/identity.rs
autonomous: true
requirements: [STOR-01]
user_setup: []

must_haves:
  truths:
    - "A KeyPackage persists to disk age-encrypted and reloads byte-equal under the same passphrase, tagged (key_id, epoch, seat)"
    - "The plaintext PublicKeyPackage envelope is readable from the store with NO unlock, reusing the existing PubkeyEnvelope format so address derivation stays single-path"
    - "The transport identity keypair persists→reloads; its npub is stable, starts with npub1, and uses bech32 (not bech32m)"
    - "No function anywhere converts a FROST KeyPackage/SigningShare/VerifyingKey to or from the IdentityKeypair — reuse is non-expressible (D-13)"
  artifacts:
    - src/store/manifest.rs
    - src/store/participant.rs
    - src/store/identity.rs
  key_links:
    - "participant store reuses crate::cli::address::PubkeyEnvelope for the plaintext public package (D-05)"
    - "share tags reuse crate::crypto::types::{KeyId, Epoch, SeatId} — not reinvented"
    - "IdentityKeypair is generated from an independent secp256k1 OsRng draw, keeping the transport key off the frost k256 curve (D-13/D-14)"
---

<objective>
Build the participant secret store (STOR-01): the file-per-share tree + plaintext manifest (D-05),
per-`(key_id, epoch, seat)` age-encrypted `KeyPackage` with decrypt-use-drop hygiene (D-06), the
plaintext public `PubkeyEnvelope` written so `address`/`share status` work with no unlock, and the
transport-only `IdentityKeypair` newtype (D-12/D-13/D-14) with npub derivation (D-15 prep).

Purpose: this is the durable home for a participant's crown-jewel share and the independent
transport identity. It must hold multiple epochs simultaneously (ROT-06 steady state ≈ 2) so Phase 4
rotation writes new epoch dirs alongside old ones, and it must enforce transport↔FROST separation
structurally — the fourth structural control in the spirit of Phase 1's four.
Output: ParticipantStore + Manifest + IdentityKeypair with persist/reload, tagging, npub, and
structural-separation tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-persistence-storage/02-CONTEXT.md
@.planning/phases/02-persistence-storage/02-RESEARCH.md
@.planning/phases/02-persistence-storage/02-PATTERNS.md
@src/cli/address.rs
@src/crypto/types.rs
@src/crypto/nonce.rs
@src/store/mod.rs
@src/store/crypto.rs
@src/store/atomic.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest schema + versioning</name>
  <files>src/store/manifest.rs, src/store/mod.rs</files>
  <read_first>src/cli/address.rs (PubkeyEnvelope versioned-serde + from/decode + typed error, lines 61-124), .planning/phases/02-persistence-storage/02-RESEARCH.md (manifest.json schema lines 419-430; V5 Input Validation), .planning/phases/02-persistence-storage/02-PATTERNS.md (store/manifest.rs section)</read_first>
  <behavior>
    - Adding a ShareEntry then serializing→deserializing the Manifest preserves (key_id, epoch, seat, state, created_at) exactly (test: tags).
    - Loading a manifest whose schema_version is unknown/newer returns Err, not a silent misparse.
    - Looking up by (key_id, epoch, seat) finds the entry; removing it drops it.
  </behavior>
  <action>
    Add `pub mod manifest;` to src/store/mod.rs (and a `pub use manifest::Manifest;`). Create
    src/store/manifest.rs following the PubkeyEnvelope versioned-serde idiom: a `Manifest` struct with
    a `schema_version: u32` field (playing the same forward-compat role as PubkeyEnvelope's
    `#[serde(default)] epoch`) and a `shares: Vec<ShareEntry>` list. ShareEntry carries key_id (String),
    epoch (u64), seat (hex of the frost Identifier — matching the coordinator roster identifier),
    state (an enum ACTIVE | STANDBY | RETIRED, mirroring ROT-06/LIFE-03), and created_at (unix seconds).
    Per D-05 the manifest indexes ONLY the encrypted shares — the identity keypair and per-(key_id,epoch)
    public PubkeyEnvelope live at well-known paths, NOT in the manifest. Provide add/lookup/remove-entry
    methods and a load method that validates schema_version and rejects unknown/newer versions (RESEARCH
    V5), all returning StoreError. Reuse crate::crypto::types::{KeyId, Epoch, SeatId} for the tag tuple;
    do not reinvent (D-02 tag lives inside each payload AND is indexed here). Add unit test `tags`.
  </action>
  <verify>
    <automated>cargo test --lib store::manifest::tests</automated>
  </verify>
  <acceptance_criteria>Manifest roundtrips (key_id, epoch, seat, state, created_at); unknown schema_version is rejected; add/lookup/remove work; reuses existing tagging newtypes.</acceptance_criteria>
  <done>src/store/manifest.rs compiles with the tags test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ParticipantStore — encrypted shares + plaintext public package</name>
  <files>src/store/participant.rs, src/store/mod.rs</files>
  <read_first>src/cli/address.rs (PubkeyEnvelope::from_package/decode_package; address_from_pubkey_file read→deserialize→decode, lines 104-136), src/store/crypto.rs, src/store/atomic.rs, src/store/manifest.rs, .planning/phases/02-persistence-storage/02-PATTERNS.md (store/participant.rs section; data-flow lines 128), .planning/phases/02-persistence-storage/02-RESEARCH.md (Pattern 1; manifest schema)</read_first>
  <behavior>
    - put_share(tag, key_package) then load_share(tag) with the same passphrase returns a KeyPackage equal to the original (test: share_roundtrip via an in-code PassphraseSource).
    - After put_share, the plaintext PubkeyEnvelope under pubkey/<key_id>/epoch-<N>.json is readable and decodes to the group PublicKeyPackage with NO passphrase.
    - load_share returns the decrypted material inside a Zeroizing scope that drops at operation end.
  </behavior>
  <action>
    Add `pub mod participant;` to src/store/mod.rs and `pub use participant::ParticipantStore;`.
    Create src/store/participant.rs: a `ParticipantStore` holding the resolved store root and a
    `Box<dyn PassphraseSource>` (D-03). Secret write path (D-05/D-06/D-07): serialize the KeyPackage via
    frost `.serialize()`, wrap in Zeroizing, `crypto::encrypt_secret`, then `atomic::write_atomic` to
    `shares/<key_id>/epoch-<N>/seat-<NNNN>.age` at mode 0600, then update manifest.json LAST (so it never
    points at a half-written file, D-07). Secret read path mirrors address_from_pubkey_file but inserts
    decrypt: read bytes → `crypto::decrypt_secret` (→ Zeroizing<Vec<u8>>) → `KeyPackage::deserialize`,
    dropping the Zeroizing buffer the instant the op ends (D-06). Public path: reuse
    crate::cli::address::PubkeyEnvelope::from_package + serde_json to write the PLAINTEXT public envelope
    under `pubkey/<key_id>/epoch-<N>.json` (D-05, continuing Phase 1 D-09) so address/share-status work
    from the store with no unlock; provide a load_public_envelope reader that needs no passphrase. The
    store must support multiple epochs held simultaneously (write new epoch dirs alongside old; do not
    delete old epochs — active pruning is Phase 4). Tag with crate::crypto::types::{KeyId, Epoch, SeatId}.
    Add unit test `share_roundtrip` driving an in-code PassphraseSource and asserting the public envelope
    reads with no passphrase.
  </action>
  <verify>
    <automated>cargo test --lib store::participant::tests</automated>
  </verify>
  <acceptance_criteria>KeyPackage persist→reload is byte-equal under one passphrase; manifest updated last; plaintext PubkeyEnvelope readable with no unlock via the reused address.rs format; multiple epochs coexist; decrypted material is Zeroizing-scoped.</acceptance_criteria>
  <done>src/store/participant.rs compiles with share_roundtrip green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IdentityKeypair newtype + npub (structural transport↔FROST separation)</name>
  <files>src/store/identity.rs, src/store/mod.rs</files>
  <read_first>src/crypto/nonce.rs (whole file — the canonical structural-control newtype; doc lines 11-27), .planning/phases/02-persistence-storage/02-RESEARCH.md (Code Examples lines 300-329; Pitfall 3, 4, 5), .planning/phases/02-persistence-storage/02-PATTERNS.md (store/identity.rs section — exact structural-control match)</read_first>
  <behavior>
    - IdentityKeypair::generate() then persist→reload under the store passphrase yields the same npub (test: identity_roundtrip_npub).
    - npub() starts with "npub1" and encodes the 32 x-only bytes with the Bech32 (NOT Bech32m) checksum.
    - Structural guard: there is no From/TryFrom/conversion between IdentityKeypair and any FROST type (KeyPackage/SigningShare/VerifyingKey) — reuse is non-expressible.
  </behavior>
  <action>
    Add `pub mod identity;` to src/store/mod.rs and `pub use identity::IdentityKeypair;`. Create
    src/store/identity.rs mirroring the EphemeralNonces structural-control pattern (D-13). Define
    `pub struct IdentityKeypair { secret: Zeroizing<[u8; 32]> }`. `generate()` builds a Secp256k1 context
    and draws from an INDEPENDENT `secp256k1::rand::rngs::OsRng` via `generate_keypair`, storing the
    32-byte secret in Zeroizing (secp256k1::SecretKey is NOT ZeroizeOnDrop — keep the raw bytes in
    Zeroizing and construct SecretKey::from_slice only inside a short scope, Pitfall 5). `npub()` derives
    the x-only pubkey and encodes it with `bech32::encode::<bech32::Bech32>(Hrp::parse("npub"), &xonly_bytes)`
    — NOT Bech32m (Pitfall 4); the crate does the 8→5-bit conversion. Provide persist (age-encrypt the raw
    secret via crypto::encrypt_secret + atomic::write_atomic to the identity slot, e.g. identity.age) and
    reload (decrypt→Zeroizing→reconstruct) under the SAME store passphrase (D-02, D-12). The load-bearing
    rule (D-13): provide NO From/TryFrom/conversion to or from any FROST type — reuse of FROST material as
    the identity key must be non-expressible, exactly as reusing a nonce is. Document the control by
    pointing at the reviewable structural guard, not a runtime assertion (mirror nonce.rs). Add unit test
    `identity_roundtrip_npub` (stable npub across persist/reload, starts npub1) and a structural guard test
    asserting no FROST↔identity conversion exists (a compile-fail trybuild snapshot or a documented type-level
    review artifact). This IdentityKeypair::npub() feeds the coordinator roster's real npubs in 02-04 (D-15).
  </action>
  <verify>
    <automated>cargo test --lib store::identity::tests</automated>
  </verify>
  <acceptance_criteria>Identity keypair persist→reload yields a stable npub starting npub1 via Bech32 (not Bech32m); secret held in Zeroizing; NO conversion fn between IdentityKeypair and any FROST type exists; independent OsRng draw on the secp256k1 curve.</acceptance_criteria>
  <done>src/store/identity.rs compiles with identity_roundtrip_npub green and the structural-separation guard in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| encrypted share file → disk | Share plaintext must never touch disk unencrypted; manifest must never index an incomplete file |
| plaintext public package → any reader | Public PublicKeyPackage is intentionally readable with no unlock; must contain no secret |
| FROST material ↔ transport identity | The two secp256k1 keys must never be derivable from one another |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Spoofing / Information Disclosure | identity key derived from / reused as FROST material | high | mitigate | Distinct newtype, independent OsRng, NO conversion fn (D-13, Pitfall 3); structural guard test |
| T-02-06 | Information Disclosure | KeyPackage plaintext written unencrypted or lingering in RAM | high | mitigate | encrypt_secret before write; decrypt returns Zeroizing dropped at op end (D-06) |
| T-02-07 | Tampering | manifest points at a half-written share after crash | high | mitigate | Atomic write then manifest updated LAST (D-07) |
| T-02-08 | Spoofing | wrong bech32 variant yields an invalid/rejected npub | medium | mitigate | bech32 Bech32 (not Bech32m), HRP npub (Pitfall 4) |
| T-02-09 | Information Disclosure | secp256k1::SecretKey not zeroized on drop | medium | mitigate | Raw secret kept in Zeroizing<[u8;32]>; SecretKey built in short scope (Pitfall 5) |
</threat_model>

<verification>
- `cargo test --lib store::manifest:: store::participant:: store::identity::` green.
- Public PubkeyEnvelope reads with no passphrase (reused single-path format).
- `cargo clippy -- -D warnings` clean.
- Manual (pre-verify): a freshly created store's public package derives an address with no unlock.
</verification>

<success_criteria>
A participant can hold identity + per-(key_id, epoch, seat) shares age-encrypted at rest, reload them byte-equal under one passphrase, and expose a plaintext public package for unlock-free address/status — with transport↔FROST separation enforced structurally.
</success_criteria>

<output>
Create `.planning/phases/02-persistence-storage/02-02-SUMMARY.md` when done.
</output>
