---
phase: 01-crypto-bridge-in-process-signing
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/crypto/mod.rs
  - src/crypto/keygen.rs
  - src/crypto/nonce.rs
  - src/crypto/types.rs
  - src/cli/keygen.rs
  - tests/dkg_small.rs
  - tests/dkg_100_correctness.rs
  - tests/ui/nonce_no_serialize.rs
  - tests/compile_fail.rs
autonomous: true
requirements: [KEY-01, KEY-02, KEY-05, KEY-06, SIGN-05]
must_haves:
  truths:
    - "An in-process DKG generic over (t,n) produces a KeyPackage+PublicKeyPackage whose verifying key is the Taproot internal key P, normalized even-Y (KEY-01)"
    - "The same routines run fully in-process with all seats simulated, no transport (KEY-02)"
    - "Every simulated seat's verifying key is confirmed equal to the group key; any mismatch aborts the ceremony (KEY-05)"
    - "The full n=100 share set produces 100 KeyPackages that all verify to one group PublicKeyPackage, with O(n²) part1/part2/part3 timing + peak-RSS measured (KEY-06, D-03)"
    - "The signing-nonce type cannot be serialized/persisted — a persistence attempt fails to compile (SIGN-05)"
  artifacts:
    - "src/crypto/keygen.rs — dkg part1/2/3 simulation loop + into_even_y(None) + verify-to-group"
    - "src/crypto/nonce.rs — non-serializable EphemeralNonces newtype (Zeroizing inner)"
    - "tests/dkg_100_correctness.rs — #[ignore] KEY-06 correctness + O(n²) instrumentation"
    - "tests/ui/nonce_no_serialize.rs + tests/compile_fail.rs — trybuild compile-fail proof"
  key_links:
    - "keys::dkg::part3 output → into_even_y(None) → PublicKeyPackage.verifying_key() == bridge input"
    - "round1::commit consumed by EphemeralNonces::commit; EphemeralNonces consumed by-value in sign() (nonce dropped)"
---

<objective>
Build the pure crypto core over `frost-secp256k1-tr` 3.0.0: the in-process DKG (generic over
(t,n), real target 51-of-100), even-Y normalization, client-side key confirmation, the
n=100 correctness proof with O(n²) instrumentation (KEY-06, folded in per D-03), and the
single highest-severity structural control — a signing-nonce type the compiler refuses to
persist (SIGN-05).

Purpose: prove the DKG produces one group key = internal key P that the bridge (01-01) can turn
into the P2TR address, and make nonce persistence a compile error from the first line of signing
code (Pitfall 1 — the key-extraction bug class).
Output: `crypto/` (pure, no I/O), the `EphemeralNonces` type, the keygen command, and the
correctness/compile-fail tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crypto-bridge-in-process-signing/01-CONTEXT.md
@.planning/phases/01-crypto-bridge-in-process-signing/01-RESEARCH.md
@./.claude/CLAUDE.md
@src/bridge/taproot.rs
</context>

<artifacts_produced>
## Artifacts this phase produces (created by THIS plan)

- `src/crypto/keygen.rs` — `fn run_inprocess_dkg(t, n)` → `(BTreeMap<Identifier, KeyPackage>, PublicKeyPackage)`; even-Y normalization; verify-to-group + mismatch-abort
- `src/crypto/nonce.rs` — `struct EphemeralNonces` (no Serialize/Deserialize, Zeroizing inner) with `commit(...)` and `sign(...)` (consumes self)
- `src/crypto/types.rs` — `(key_id, epoch, identifier)` newtypes
- `src/cli/keygen.rs` — keygen command wiring (writes `PublicKeyPackage` public artifact, D-09)
- `tests/dkg_small.rs`, `tests/dkg_100_correctness.rs`, `tests/ui/nonce_no_serialize.rs`, `tests/compile_fail.rs`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-serializable signing-nonce type + trybuild compile-fail proof (SIGN-05)</name>
  <files>src/crypto/nonce.rs, src/crypto/mod.rs, tests/ui/nonce_no_serialize.rs, tests/compile_fail.rs</files>
  <read_first>
    - src/crypto/nonce.rs (this file — created here)
    - .planning/research/PITFALLS.md Pitfall 1 (persisting/reusing nonces — key-extraction bug class)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-RESEARCH.md (Pattern 3, Code Examples §Nonce, Assumption A6)
    - SPEC-frost-cli.md §6.5 (signing / nonce discipline) and §11 (security considerations)
    - ./.claude/CLAUDE.md (FROST 3.0: SigningKey ZeroizeOnDrop; round1::commit; round2::sign_with_tweak)
  </read_first>
  <behavior>
    - `EphemeralNonces` wraps `frost::round1::SigningNonces`, holds Zeroizing state, and implements NEITHER Serialize NOR Deserialize (nor a Clone that outlives a round).
    - `EphemeralNonces::commit(signing_share, rng)` returns `(EphemeralNonces, SigningCommitments)` by wrapping `round1::commit`.
    - `EphemeralNonces::sign(self, signing_package, key_package)` consumes self by value (dropping the nonce) and returns a `SignatureShare` via `round2::sign_with_tweak`.
    - Any attempt to serialize an `EphemeralNonces` value fails to compile (trybuild `.stderr` snapshot).
  </behavior>
  <action>
    In `src/crypto/nonce.rs` define `struct EphemeralNonces(frost::round1::SigningNonces)` — move-only,
    with NO derive of a serialization trait and NO serde impl, inner state wrapped for zeroization
    per `zeroize`. Provide `commit(share: &frost::keys::SigningShare, rng: &mut impl RngCore)
    -> (Self, frost::round1::SigningCommitments)` wrapping `frost::round1::commit`, and
    `sign(self, pkg: &frost::SigningPackage, kp: &frost::keys::KeyPackage)
    -> Result<frost::round2::SignatureShare, frost::Error>` which consumes `self` and calls
    `frost::round2::sign_with_tweak` (self dropped ⇒ nonce gone). Add `tests/ui/nonce_no_serialize.rs`
    that attempts `serde_json::to_vec(&nonce)` on an `EphemeralNonces` and MUST fail to compile, and
    `tests/compile_fail.rs` driving it via `trybuild::TestCases::new().compile_fail(...)`. Do NOT put
    the phrase the compile-fail test asserts against anywhere in `src/`; the proof is the trybuild
    snapshot, not a code comment. Reference SIGN-05 in the module doc.
  </action>
  <verify>
    <automated>set -o pipefail; cargo test --test compile_fail 2>&1 | tail -5 && test "$(grep -rl 'impl Serialize for EphemeralNonces\|derive(Serialize' src/crypto/nonce.rs | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test compile_fail` exits 0 (the compile-fail case is correctly rejected)
    - `src/crypto/nonce.rs` has no `Serialize`/`Deserialize` derive or impl for `EphemeralNonces`
    - `EphemeralNonces::sign` takes `self` by value
  </acceptance_criteria>
  <done>The nonce type is structurally non-persistable; a serialization attempt is a compile error proven by trybuild (SIGN-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: In-process DKG (generic over t,n) + even-Y + client-side confirmation + keygen command</name>
  <files>src/crypto/keygen.rs, src/crypto/types.rs, src/crypto/mod.rs, src/cli/keygen.rs, tests/dkg_small.rs</files>
  <read_first>
    - src/crypto/keygen.rs (this file — created here)
    - src/bridge/taproot.rs (address_from_group_key — for the group-key→address assertion)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-RESEARCH.md (Code Examples §DKG, Pattern 2 even-Y, Pattern 5 confirmation, Assumptions A2/A3)
    - SPEC-frost-cli.md §3 (group key = internal key P) and §6.1 (Keygen — DKG mode)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-CONTEXT.md (D-01 generic (t,n); D-02 real target; D-09 public-artifact file)
  </read_first>
  <behavior>
    - `run_inprocess_dkg(t, n)` runs part1/part2/part3 across n simulated seats in-process (no transport) and returns a `BTreeMap<Identifier, KeyPackage>` plus one group `PublicKeyPackage`.
    - Both packages are normalized via `into_even_y(None)`; `pubkeys.verifying_key().has_even_y()` holds.
    - Every seat's `KeyPackage` verifying key equals the group verifying key; a deliberately corrupted seat key makes confirmation FAIL and abort (KEY-05).
    - The group verifying key feeds `bridge::address_from_group_key` successfully (proves DKG→bridge wiring at small n).
    - `cheget keygen` (simulate-all-seats mode) writes the `PublicKeyPackage` public artifact to a plaintext file (D-09); no secret share touches disk.
  </behavior>
  <action>
    In `src/crypto/keygen.rs` implement `fn run_inprocess_dkg(min_signers: u16, max_signers: u16)`
    following RESEARCH Code Examples §DKG: round-1 `keys::dkg::part1` per seat into BTreeMaps;
    round-2 `keys::dkg::part2` distributing round2 packages by recipient; round-3 `keys::dkg::part3`
    per seat; apply `.into_even_y(None)` to each `KeyPackage` and `PublicKeyPackage` (Pattern 2, D-11).
    Assert all seats agree on `verifying_key()` (KEY-06 correctness invariant) and expose a
    `confirm_group_key(&packages, &group) -> Result<(), KeygenError>` that aborts on any mismatch
    (KEY-05). Keep the module PURE — no chain/transport/filesystem imports in `crypto/`. Add
    `src/crypto/types.rs` with `(key_id, epoch, identifier)` newtypes for downstream tagging. Wire
    `src/cli/keygen.rs` to call `run_inprocess_dkg` at a caller-supplied `(t,n)` (default the real
    51/100 gated behind a flag; small default for interactive use), serialize the resulting
    `PublicKeyPackage` to the D-09 public-artifact envelope, and write it to the `--out` path.
    Add `tests/dkg_small.rs`: a 3-of-5 DKG asserting verifying-key agreement, that
    `bridge::address_from_group_key` succeeds on the group key, and that a corrupted-seat
    confirmation aborts (KEY-01/02/05).
  </action>
  <verify>
    <automated>set -o pipefail; cargo test --test dkg_small 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test dkg_small` exits 0
    - `run_inprocess_dkg` returns a group key with `has_even_y() == true`
    - A corrupted-seat confirmation test asserts the ceremony aborts (KEY-05)
    - `src/crypto/` imports no chain/transport/filesystem modules (purity)
    - `cheget keygen` writes a `PublicKeyPackage` artifact readable by `cheget address --pubkey`
  </acceptance_criteria>
  <done>An in-process (t,n) DKG yields one even-Y group key = internal key P, confirmed client-side, feeding the bridge; the keygen command emits a public artifact (KEY-01, KEY-02, KEY-05).</done>
</task>

<task type="auto">
  <name>Task 3: n=100 correctness proof + O(n²) timing/RSS instrumentation (KEY-06, D-03)</name>
  <files>tests/dkg_100_correctness.rs</files>
  <read_first>
    - tests/dkg_100_correctness.rs (this file — created here)
    - src/crypto/keygen.rs (run_inprocess_dkg from Task 2)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-RESEARCH.md (Open Questions Q1 n=100 feasibility, Performance reasoning; Code Examples §n=100 loop)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-VALIDATION.md (Manual-Only Verifications: n=100 measurement is the deliverable)
    - .planning/phases/01-crypto-bridge-in-process-signing/01-CONTEXT.md (D-03 fold-in, D-06 nightly gate)
  </read_first>
  <action>
    Add `tests/dkg_100_correctness.rs` marked `#[ignore]` (nightly/on-demand per D-06) that runs
    `run_inprocess_dkg(51, 100)` and asserts all 100 `KeyPackage`s verify to a single group
    `PublicKeyPackage` (KEY-06 correctness). Instrument and print (via `--nocapture`) per-part
    wall-clock for part1/part2/part3 and peak RSS across the 100 seats (the O(n²) measurement is
    itself the deliverable per D-03 — pass/fail is feasibility, not a fixed threshold). Recommend
    running in `--release`; optionally parallelize only the simulation loop (per-seat crypto is
    independent) while keeping per-seat calls deterministic. Do not add persistence (D-09 — the
    persist/reload-at-scale check is Phase 2 per D-04).
  </action>
  <verify>
    <automated>set -o pipefail; cargo test --test dkg_100_correctness -- --ignored --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test dkg_100_correctness -- --ignored` exits 0 (all 100 KeyPackages verify to one group key)
    - The test emits per-part (part1/part2/part3) wall-clock and peak-RSS numbers to stdout
    - The test is `#[ignore]` so it does not run on the per-PR quick gate (D-06)
  </acceptance_criteria>
  <done>The full n=100 in-process DKG is proven correct (one group key from 100 shares) and its O(n²) compute cost is measured locally (KEY-06, D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signing-nonce lifetime → any store/serde surface | Nonces must never cross a persistence boundary (Pitfall 1) |
| per-seat DKG output → group key agreement | A dishonest/corrupted seat key must be caught by client-side confirmation |
| secret share material → disk | Only public artifacts persist in Phase 1 (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-nonce | Information Disclosure / Elevation | `crypto/nonce.rs` EphemeralNonces | critical | mitigate | Non-serializable, move-only, Zeroizing newtype; consumed by-value in sign(); trybuild compile-fail proof (SIGN-05) |
| T-01-keyconfirm | Tampering | DKG group-key agreement | high | mitigate | Client-side `confirm_group_key` asserts every seat's key == group key; mismatch aborts (KEY-05) |
| T-01-secretdisk | Information Disclosure | keygen artifact write | high | mitigate | Only the public `PublicKeyPackage` is written (D-09); secret shares stay in-process; `SigningKey: ZeroizeOnDrop` |
</threat_model>

<verification>
- `cargo test --test compile_fail` green (nonce non-serializable).
- `cargo test --test dkg_small` green (DKG correctness + even-Y + confirmation abort).
- `cargo test --test dkg_100_correctness -- --ignored` green with printed O(n²) measurements.
- `crypto/` has no I/O imports.
</verification>

<success_criteria>
An in-process (t,n) DKG produces one even-Y group key = internal key P, every seat confirms it,
the n=100 set is proven correct with measured O(n²) cost, and the nonce type is compile-time
non-persistable (KEY-01, KEY-02, KEY-05, KEY-06, SIGN-05).
</success_criteria>

<output>
Create `.planning/phases/01-crypto-bridge-in-process-signing/01-02-SUMMARY.md` when done.
</output>
