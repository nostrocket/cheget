---
phase: quick-260713-pk2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260713-pk2
must_haves:
  truths:
    - "Every CLI command printed in README.md has actually been run by the executor (at minimum `--help`); the address-derivation command is run end-to-end against a real pubkey file."
    - "README documents a 'how to use on regtest' section and a 'how to use on mainnet' section, both grounded only in behavior that exists today."
    - "README honestly separates what works today (Phase 1, in-process) from what is planned (Phases 2-7); no fabricated transport/persistence/rotation/watch/sweep workflow is presented as usable."
    - "The mainnet section documents ONLY offline address derivation as usable today and prominently states the full mainnet signing/custody ceremony is not yet wired."
    - "README uses the current name `cheget` and the current fixed threshold 51-of-100; no `tsig`, no `501`/`1000`."
  artifacts:
    - README.md
  key_links:
    - "README commands map exactly to the real clap surface in src/cli/{mod,address,keygen,sign}.rs and src/main.rs."
    - "Regtest test-harness instructions map to tests/inproc_sign_100.rs and tests/dkg_100_correctness.rs as they exist today (run by default, release recommended)."
---

<objective>
Bring the repo-root `README.md` fully up to date and add accurate "how to use on
mainnet" and "how to use on regtest" sections. This is a real Bitcoin signing tool:
ACCURACY is paramount. The executor MUST ground every documented command in reality
by reading the CLI source and running the actual binary — never by guessing flags or
inventing workflows that do not exist.

Only Phase 1 is implemented: the crypto bridge, in-process DKG, in-process two-round
signing to a confirmed regtest key-spend, a `Transport` trait + in-memory stub, and a
`ChainBackend` trait. There is NO transport layer, NO persistence, NO membership
rotation, and NO live watch/sweep (Phases 2-7, not built).

Purpose: a truthful README that a prospective user can follow without hitting
commands, flags, or capabilities that do not exist.
Output: an updated `README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@src/main.rs
@src/cli/mod.rs
@src/cli/address.rs
@src/cli/keygen.rs
@src/cli/sign.rs
@tests/inproc_sign_100.rs
@tests/dkg_100_correctness.rs
</context>

<critical_ground_truth>
The following was confirmed by reading source before planning. The executor MUST
re-verify by running the CLI, but this is the expected reality — do not regress the
README to an older, less accurate state:

- The current README is STALE about the CLI surface. It claims only
  `cheget watcher address` is wired and that `keygen`/`sign` are test-only. That is
  no longer true. `src/cli/mod.rs` dispatches real handlers:
  - `cheget participant keygen` and `cheget coordinator keygen` → `keygen::run`
  - `cheget participant sign` and `cheget coordinator sign` → `sign::run`
  - `cheget watcher address` → `address::run`
  (The doc-comment at the top of `src/cli/mod.rs` still says keygen/sign are
  `unimplemented` stubs — that comment is stale; trust the dispatch code and the
  actual `--help`/run output, not the comment.)

- `keygen` runs an in-process simulate-all-seats DKG (D-08) and writes ONLY the
  public `PublicKeyPackage` envelope (D-09); default 3-of-5, `--full` = 51-of-100.
  It is NOT a multi-party ceremony over any network — document it as in-process.

- `sign` runs an in-process simulate-all-seats DKG, derives the group address, and
  drives a two-round signing session over the supplied `--psbt`. Default network is
  `regtest`. Because shares are not persisted (D-09), the PSBT must spend the
  address this in-process run derives — it is a self-contained demo of the pipeline,
  NOT signing of an externally-funded, persisted-key wallet. Represent it that way.

- `watcher address` flags (confirmed in `src/cli/address.rs`): `--pubkey <file>`
  (required) and `--network <bitcoin|testnet|signet|regtest>` (default `bitcoin`).
  The `Network` enum accepts exactly: `bitcoin`, `testnet`, `signet`, `regtest`.

- `tests/inproc_sign_100.rs` and `tests/dkg_100_correctness.rs` now run BY DEFAULT
  (no `#[ignore]`). The current README's claim that the crown-jewel test is
  `#[ignore]` and must be run with `--ignored` is WRONG and must be corrected.
  Release is strongly recommended for speed.

- Env overrides for the full-scale tests: `CHEGET_SIGN_T` / `CHEGET_SIGN_N` (signing
  test) and `CHEGET_DKG_T` / `CHEGET_DKG_N` (DKG test).

- The crate/binary is `cheget`. It is NOT published — do NOT document
  `cargo install`, crates.io, or homebrew. Build from source only.
</critical_ground_truth>

<tasks>

<task type="auto">
  <name>Task 1: Discover and capture the real CLI surface by running the binary</name>
  <files>README.md</files>
  <action>
Do NOT edit README.md in this task — this is pure discovery whose captured output
grounds Task 2. Read src/main.rs and all of src/cli/ (mod.rs, address.rs, keygen.rs,
sign.rs) if not already in context, then run the actual binary and record verbatim
output for every command that will appear in the README.

Run and capture (build first so subprocess runs are not mixed with compile noise):
  1. `cargo build` — confirm the tree compiles before documenting anything.
  2. `cargo run -- --help` — top-level persona tree.
  3. `cargo run -- participant --help`, `cargo run -- coordinator --help`,
     `cargo run -- watcher --help` — subcommand lists per persona.
  4. `cargo run -- watcher address --help` — confirm `--pubkey` and the exact
     accepted `--network` values.
  5. `cargo run -- participant keygen --help` and `cargo run -- participant sign --help`
     (coordinator variants share the same arg structs — note that, do not re-run all).

Then exercise the one command that can be fully demonstrated offline end-to-end:
  6. Create a real public-artifact file with the working keygen path, into the
     scratchpad (NOT the repo), e.g. a small fast key:
       `cargo run -- coordinator keygen --key-id readme-demo --out <scratch>/pk.json`
     Confirm it writes a JSON envelope with `key_id`, `epoch`, `pubkey_package_hex`.
  7. Derive the address for regtest and for mainnet from that real file:
       `cargo run -- watcher address --pubkey <scratch>/pk.json --network regtest`
       `cargo run -- watcher address --pubkey <scratch>/pk.json --network bitcoin`
     Record the real `bcrt1p...` and `bc1p...` outputs (they will differ per random
     DKG run — capture the shape and the command, and note addresses are per-key).

Record, for use in Task 2: the exact set of personas, subcommands, and flags that
truly exist; which are fully usable offline today (address derivation; in-process
keygen writing a public package; in-process sign as a self-contained pipeline demo);
and confirm keygen/sign are in-process simulate-all-seats (no transport). If any
command errors or behaves as an unfinished stub, note that honestly for Task 2 —
never write a usage example for a path that does not run.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5; cargo run -- watcher address --help 2>&1 | grep -iE 'network|pubkey'</automated>
  </verify>
  <done>
`cargo build` succeeds. The real `--help` output for the top level, all three
personas, and the address/keygen/sign subcommands has been captured, and a real
pubkey file was created and turned into both a regtest and a mainnet address via the
binary. The executor knows exactly which commands, flags, and network values exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md grounded in the captured surface, add mainnet + regtest sections, and verify</name>
  <files>README.md</files>
  <action>
Rewrite README.md so it is fully current and truthful, using ONLY the commands,
flags, personas, and network values captured in Task 1. Preserve the existing honest
tone and the "works today (Phase 1) vs planned (Phases 2-7)" structure, but correct
every stale claim.

Required corrections and additions:

1. CLI-status correction. The current README says only `watcher address` is wired
   and that keygen/sign are test-only. Update it to reflect that `keygen` and `sign`
   are now real CLI commands under both `participant` and `coordinator`, while being
   precise that they run an IN-PROCESS simulate-all-seats DKG (no transport, no
   persistence) — keygen writes only the public key package; sign is a self-contained
   demonstration of the two-round signing pipeline over a supplied PSBT that must
   spend the in-process-derived address. Do not overstate these as multi-party or
   network-backed.

2. Test-suite correction. Remove the claim that the crown-jewel regtest key-spend
   test is `#[ignore]` / must be run with `--ignored`. Document that both full-scale
   tests (`tests/dkg_100_correctness.rs` and `tests/inproc_sign_100.rs`) now run by
   default under `cargo test`, that each auto-spawns a throwaway regtest bitcoind via
   the `corepc-node` dev-dependency (the signing test), and that the release profile
   is strongly recommended (mention the measured contrast: ~9s release vs ~90s debug
   for the confirmed regtest key-spend). Present timings as local measurements, not
   benchmarks.

3. "How to use on regtest" section (new, prominent). Document only what genuinely
   works on regtest today:
     - the end-to-end confirmed key-spend proof via the test harness
       (`cargo test --release --test inproc_sign_100 -- --nocapture`), noting it
       auto-spawns bitcoind and needs no manual node;
     - deriving a regtest address with the address command using the verified
       regtest network value (`--network regtest`, yields `bcrt1p...`);
     - the in-process `sign` pipeline demo (default network regtest) as a way to run
       the two-round signing flow locally, being explicit it is self-contained.
     If there is no real CLI path that talks to a live regtest node, state plainly
     that live signing is exercised through the test harness, not a standing daemon.

4. "How to use on mainnet" section (new, prominent). Document ONLY what is genuinely
   possible today: offline derivation of the group's mainnet P2TR address from a
   PublicKeyPackage via the address command with the mainnet network value
   (`--network bitcoin`, yields `bc1p...`), emphasizing it makes no network calls and
   is safe to run. State CLEARLY and prominently that the full mainnet signing
   ceremony — multi-party rounds over transport, PSBT signing, broadcast, watch,
   sweep — is NOT yet wired and arrives in later phases. Do NOT invent a mainnet
   spend/custody workflow. Include a short, prominent safety/status note: Phase 1
   only, not production-audited, do not entrust real funds to flows that do not exist.

5. General currency refresh:
     - Name is `cheget` (crate + binary) throughout.
     - Fixed threshold is 51-of-100 (t=51, n=100).
     - Document the scaling env override vars where the full-scale tests are
       described: the signing test honors CHEGET_SIGN_T / CHEGET_SIGN_N and the DKG
       test honors CHEGET_DKG_T / CHEGET_DKG_N.
     - Build/test instructions: `cargo build`, `cargo build --release`, `cargo test`,
       and the two full-scale tests (which run by default; release recommended). Do
       NOT document `cargo install cheget`, crates.io, or homebrew — not published;
       build from source.
     - Keep the honest done (Phase 1) vs planned (Phases 2-7) split intact and
       accurate; keep the existing "Security model (current)" claims that the code
       actually enforces.

Keep every fenced code block balanced and every documented command copy-pasteable
and real. Every command in the final README must be one the executor ran in Task 1.
  </action>
  <verify>
    <automated>test -z "$(grep -in 'tsig' README.md)" && echo NO_TSIG_OK; test -z "$(grep -nE '\b(501|1000)\b' README.md)" && echo NO_OLD_PARAMS_OK; test $(( $(grep -c '^```' README.md) % 2 )) -eq 0 && echo FENCES_BALANCED_OK; grep -q -- '--network regtest' README.md && grep -q -- '--network bitcoin' README.md && echo NETWORK_SECTIONS_OK; grep -q 'CHEGET_SIGN_T' README.md && grep -q 'CHEGET_DKG_T' README.md && echo ENV_VARS_OK; cargo build 2>&1 | tail -3</automated>
  </verify>
  <done>
README.md is current and truthful: it uses `cheget` and 51-of-100 only; documents
the real persona/subcommand/flag surface (including the now-wired in-process
keygen/sign) exactly as run in Task 1; contains distinct "how to use on regtest" and
"how to use on mainnet" sections grounded only in what works today; the mainnet
section documents only offline address derivation as usable and prominently flags
that full mainnet signing/custody is not yet wired; the crown-jewel test is described
as running by default (not `#[ignore]`); env override vars are documented; no
`cargo install`/crates.io/homebrew claims; all fences balanced; `cargo build`
succeeds. All verify gates print their OK sentinels.
  </done>
</task>

</tasks>

<verification>
Phase-level checks (all run against the repo root):
- `grep -in 'tsig' README.md` → empty (old project name fully removed).
- `grep -nE '\b(501|1000)\b' README.md` → empty (old threshold parameters fully removed).
- Fenced code blocks balanced: even count of ``` fence lines.
- README contains both `--network regtest` and `--network bitcoin` usage.
- README documents `CHEGET_SIGN_T` and `CHEGET_DKG_T` override vars.
- No occurrence of `cargo install`, `crates.io`, or `homebrew` in README.
- `cargo build` still succeeds after the README change.
- Manual: every fenced command in README corresponds to a command the executor
  actually ran in Task 1 (no invented flags or personas).
</verification>

<success_criteria>
- README.md is accurate to the current `cheget` codebase: correct name, correct
  fixed threshold (51-of-100), correct CLI surface, correct test behavior.
- New, prominent "how to use on regtest" and "how to use on mainnet" sections exist,
  each documenting ONLY behavior that exists today.
- Mainnet section documents offline address derivation as the only usable mainnet
  action today and prominently states full signing/custody is not yet wired.
- Honest "done (Phase 1) vs planned (Phases 2-7)" split preserved; no fabricated
  transport/persistence/rotation/watch/sweep workflow presented as usable.
- All automated verify gates pass and `cargo build` succeeds.
</success_criteria>

<output>
Create `.planning/quick/260713-pk2-update-readme-with-mainnet-and-regtest-u/260713-pk2-SUMMARY.md` when done.
</output>